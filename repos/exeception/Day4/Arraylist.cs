using System;
using System.Collections.Generic;

namespace Day4
{
    /*public class Employee
    {
        public int ID;
        public string Name;
        public string Gender;

        public Employee(int id, string name, string gender)
        {
            ID = id;
            Name = name;
            Gender = gender;
        }
    }

    public class Arraylist
    {
        public static void Main()
        {
            List<Employee> employeeList = new List<Employee>();

            // Adding Employee objects to the list
            employeeList.Add(new Employee(102, "Shivael", "Female"));
            employeeList.Add(new Employee(193, "Shivan", "Male"));
            employeeList.Add(new Employee(104, "Shivan3", "Female"));

            // Displaying each employee's details
            foreach (var employee in employeeList)
            {
                Console.WriteLine("ID: " + employee.ID);
                Console.WriteLine("Name: " + employee.Name);
                Console.WriteLine("Gender: " + employee.Gender);
                Console.WriteLine();
            }
        }
    }*/
}
